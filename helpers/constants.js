const OrmType = {
    MONGOOSE: "mongoose"
}

const PlanInterval = {
    MONTH: 'month',
    YEAR: 'year',
}

const PricingType = {
    RECURRING: 'recurring',
    ONE_TIME: 'one_time',
}

module.exports = {
    OrmType,
    PlanInterval,
    PricingType
}